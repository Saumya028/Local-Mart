-r requirements.txt

# Phase 7 — the CI workflow (.github/workflows/ci.yml) installs this file
# to run tests/ and lint checks, in addition to the production
# dependencies above.
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.4
